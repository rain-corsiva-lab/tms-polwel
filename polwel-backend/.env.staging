# Staging Environment
NODE_ENV=staging
PORT=3001

# Database Configuration
DATABASE_URL="postgresql://staging_user:staging_password@localhost:5432/polwel_staging_db"

# JWT Configuration
JWT_SECRET=staging-jwt-secret-key-change-this
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=https://polwel-pdms.customized3.corsivalab.xyz

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=staging@corsivalab.xyz
SMTP_PASS=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=500

# Security
BCRYPT_ROUNDS=12

# Logging
LOG_LEVEL=info
