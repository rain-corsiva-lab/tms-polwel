<VirtualHost *:80>
    ServerName polwel-pdms.customized3.corsivalab.xyz
    DocumentRoot /www/wwwroot/polwelpdms/dist
    
    # Frontend - serve static files
    <Directory /www/wwwroot/polwelpdms/dist>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        
        # Handle React Router (SPA routing)
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteCond %{REQUEST_URI} !^/api/
        RewriteCond %{REQUEST_URI} !^/health
        RewriteRule . /index.html [L]
    </Directory>
    
    # API Proxy to Backend
    ProxyPreserveHost On
    
    # Health check endpoint (direct proxy)
    ProxyPass /health http://localhost:3001/health
    ProxyPassReverse /health http://localhost:3001/health
    
    # API endpoints - preserve full path including /api/ prefix
    ProxyPass /api http://localhost:3001/api
    ProxyPassReverse /api http://localhost:3001/api
    
    # Security Headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "DENY"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
    
    # Compression
    <IfModule mod_deflate.c>
        AddOutputFilterByType DEFLATE text/plain
        AddOutputFilterByType DEFLATE text/html
        AddOutputFilterByType DEFLATE text/xml
        AddOutputFilterByType DEFLATE text/css
        AddOutputFilterByType DEFLATE application/xml
        AddOutputFilterByType DEFLATE application/xhtml+xml
        AddOutputFilterByType DEFLATE application/rss+xml
        AddOutputFilterByType DEFLATE application/javascript
        AddOutputFilterByType DEFLATE application/x-javascript
        AddOutputFilterByType DEFLATE application/json
    </IfModule>
    
    # Cache static assets
    <IfModule mod_expires.c>
        ExpiresActive On
        ExpiresByType text/css "access plus 1 year"
        ExpiresByType application/javascript "access plus 1 year"
        ExpiresByType image/png "access plus 1 year"
        ExpiresByType image/jpg "access plus 1 year"
        ExpiresByType image/jpeg "access plus 1 year"
        ExpiresByType image/gif "access plus 1 year"
        ExpiresByType image/svg+xml "access plus 1 year"
        ExpiresByType image/x-icon "access plus 1 year"
        ExpiresByType application/pdf "access plus 1 month"
        ExpiresByType text/html "access plus 1 day"
    </IfModule>
    
    # Logs
    ErrorLog /www/wwwlogs/polwel_error.log
    CustomLog /www/wwwlogs/polwel_access.log combined
</VirtualHost>

# SSL Configuration (commented out until SSL certificate is ready)
# <IfModule mod_ssl.c>
#     <VirtualHost *:443>
#         ServerName polwel-pdms.customized3.corsivalab.xyz
#         DocumentRoot /www/wwwroot/polwelpdms/dist
#         
#         # SSL Configuration
#         SSLEngine on
#         SSLCertificateFile /path/to/your/certificate.crt
#         SSLCertificateKeyFile /path/to/your/private.key
#         SSLCertificateChainFile /path/to/your/ca_bundle.crt
#         
#         # Frontend - serve static files (same as HTTP)
#         <Directory /www/wwwroot/polwelpdms/dist>
#             Options -Indexes +FollowSymLinks
#             AllowOverride All
#             Require all granted
#             
#             # Handle React Router (SPA routing)
#             RewriteEngine On
#             RewriteBase /
#             RewriteRule ^index\.html$ - [L]
#             RewriteCond %{REQUEST_FILENAME} !-f
#             RewriteCond %{REQUEST_FILENAME} !-d
#             RewriteCond %{REQUEST_URI} !^/api/
#             RewriteCond %{REQUEST_URI} !^/health
#             RewriteRule . /index.html [L]
#         </Directory>
#         
#         # API Proxy to Backend (same as HTTP)
#         ProxyPreserveHost On
#         
#         # Health check endpoint (direct proxy)
#         ProxyPass /health http://localhost:3001/health
#         ProxyPassReverse /health http://localhost:3001/health
#         
#         # API endpoints - preserve full path including /api/ prefix
#         ProxyPass /api http://localhost:3001/api
#         ProxyPassReverse /api http://localhost:3001/api
#         
#         # Security Headers (same as HTTP)
#         Header always set X-Content-Type-Options "nosniff"
#         Header always set X-Frame-Options "DENY"
#         Header always set X-XSS-Protection "1; mode=block"
#         Header always set Referrer-Policy "strict-origin-when-cross-origin"
#         Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
#         
#         # Additional SSL Security
#         Header always set Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"
#         
#         # Logs
#         ErrorLog /www/wwwlogs/polwel_ssl_error.log
#         CustomLog /www/wwwlogs/polwel_ssl_access.log combined
#     </VirtualHost>
# </IfModule>

# Redirect HTTP to HTTPS (when SSL is ready)
# <VirtualHost *:80>
#     ServerName polwel-pdms.customized3.corsivalab.xyz
#     Redirect permanent / https://polwel-pdms.customized3.corsivalab.xyz/
# </VirtualHost>
